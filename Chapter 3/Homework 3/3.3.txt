3.3.5
	The time class efficiency of this algorithm would be O(n). The calcuation is as follows:
		x' - x'' n times
		n squares of previous step
		1 square root to wrap it all
		
		n + n + 1 = O(n)

	Another way to look at it is:
		sqrt(Summation(x'-x'')^2)
		sqrt((ni - zi)^2)
		sqrt(n^2)
		n

