3.3.5
	The time class efficiency of this algorithm would be O(n^2).

	The algorithm has to run through 2 array's simutaneously, meaning that the efficiency would be equal to having 2 nested summations. 2 nested summations have an efficiency of O(n^2), hence the answer above.

